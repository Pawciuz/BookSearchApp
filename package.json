{
	"name": "booksearchapp",
	"version": "1.0.0",
	"description": "App which uses two Book Apis for searching title, author, subject and cover of book.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "BookSearchApp"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"eslint": "^7.5.0"
	}
}
